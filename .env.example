# US-only runtime defaults
APP_TIMEZONE=America/New_York

# Optional: override the shipped US large-cap universe with a custom ticker file
# One ticker per line, e.g. AAPL, MSFT, NVDA
# US_UNIVERSE_FILE=/absolute/path/to/tickers.txt
